!(function (o) {
  o.topbutton = function (t) {
    var n = { htlm: "", css: void 0, scrollSpeed: 500, scrollAndShow: !1 };
    for (key in t) n[key] = t[key];
    var e = {
      width: "45px",
      height: "45px",
      background: "#343a40",
      color: "#fff",
      right: "15px",
      bottom: "15px",
      border: "2px solid #495057",
      "border-radius": "5px",
      "font-size": "17px",
    };
    if (void 0 != n.css) {
      var r = {},
        i = t.css.split(";").filter(function (o) {
          return "" != o;
        });
      for (key in i) {
        var l = i[key].split(":"),
          p = l[0].replace(/(\s*)/g, ""),
          s = l[1].replace(/(\s*)/g, "");
        r[p] = s;
      }
      for (key in r) e[key] = r[key];
    }
    var d = "";
    for (key in e) d += key + ":" + e[key] + "; ";
    var c = "";
    if (n.scrollAndShow) {
      c = "display:none;";
      var a = !0;
      o(window).scroll(function () {
        o(this).scrollTop() > 100
          ? a || (o("#topButton").fadeIn(), (a = !0))
          : a && (o("#topButton").fadeOut(), (a = !1));
      }),
        o(window).scrollTop() > 100 && (c = "display:block;");
    }
    var f =
      '<button type="button" id="topButton" style="' +
      c +
      "position:fixed; border:none; outline:none; overflow:hidden; cursor:pointer; " +
      d +
      '">' +
      n.htlm +
      "</button>";
    o("body").append(f),
      o("#topButton").click(function () {
        o("html, body").animate({ scrollTop: 0 }, parseInt(n.scrollSpeed));
      });
  };
})(jQuery);
